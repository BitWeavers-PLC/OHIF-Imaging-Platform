# Imaging Platform Extension

This extension is the product customization overlay layer.

Use it to add product-specific customization modules, UI wrappers and
brand behavior without modifying upstream extension packages.
